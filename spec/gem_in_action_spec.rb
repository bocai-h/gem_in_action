require "spec_helper"

describe GemInAction do
  it "has a version number" do
    expect(GemInAction::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
